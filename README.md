                   Redhat Pacemaker HA Cluster on AWS (RHEL9)
===========================================================================
A Red Hat Pacemaker High Availability (HA) cluster is an advanced cluster resource manager system for Red Hat Enterprise Linux (RHEL). It uses Pacemaker, Corosync, and the pcs tool to keep critical applications running without interruption.
